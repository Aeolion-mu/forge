# 复制为 .env 后填写密钥。Forge 是 LIVE-only：当前模型 provider 没有 key 会直接报错退出。
# 模型清单与默认模型在 forge.config.json 里（不含密钥）；这里只放密钥。

# 按需填对应 provider 的 key（默认模型是 deepseek/deepseek-v4-pro，填 DEEPSEEK_API_KEY 即可）
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# 可选：覆盖 forge.config.json 的默认模型，如 anthropic/claude-haiku-4-5
FORGE_MODEL=

# agent 的工作目录（文件/shell 工具的根，默认当前目录）
FORGE_WORKDIR=.
