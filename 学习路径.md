# Forge 学习路径 · 开发日志

> 这是 forge 的**学习路径记录**：每一步「为什么做、怎么做、踩了什么坑、学到什么」。
> **约定：之后每有改动，实时更新到这里**（在对应主题下追加，重大节点更新「当前状态」）。
>
> forge = 基于 `@earendil-works/pi-agent-core`（AgentHarness 层）自研的轻量编程 Agent。
> 目标：把「最新一代编程 Agent（Claude Code / Codex 那代）」的核心机制亲手实现一遍。
> 硬约束：LIVE-only（无 mock）；原地重构；尽量零新依赖、每个功能配单测。

---

## 当前状态（2026-09-08）

- **测试**：354 passed（48 个文件），typecheck clean。
- **最近**：**Skills 管理机制全链路落地（主题十二）**——物理三态 × 逻辑组合架构、索引快照注入、skill_read 工具、内置算子 skills、子 agent 共享；顺带修掉 memory_write 打穿前缀缓存的存量 bug。
- **转型**：算子开发专用 AI 编程工具（昇腾/Triton/天数/沐曦/燧原/海光/NVIDIA/AMD）。调研底稿 `docs/research/`，机制设计 `docs/skills-management-plan.md`。
- **默认模型**：主 `deepseek/deepseek-v4-pro`（reasoning 拉满）；子 agent `deepseek-v4-flash`（thinking）。
- **工具清单**：read_file / list_dir / write_file / edit_file / glob / grep / bash(沙箱) /
  apply_patch / **outline** / **repo_map** / **definition** / **references** / **hover** / **rename** /
  **diagnostics** / memory_* / **skill_read** / **spawn_subagent · subagent_steer · subagent_list · subagent_cancel**（异步/后台）。
- **code-intel 三层**：outline（语法/单文件）→ repo_map（全仓地图）→ LSP（语义/跨文件）。
- **上下文策略**：单 session append-only 吃满 DeepSeek 前缀缓存；90% 高水位单次压缩。
- **子 agent**：异步/后台（spawn 不阻塞、完成自动喂回主 agent）、flash 模型、max-turns 护栏、用量单独计量。

---

## 阶段 0：参照实现地基（早期）

先用库自带 `AgentHarness` 搭一个能跑的原型作参照，再逐块打磨。

| 主题 | 做了什么 | commit |
|---|---|---|
| Agent 原型 | 包一层 AgentHarness：会话持久化/工具/沙箱/权限/审计/telemetry | `e0b1c77` |
| 工具集 | glob/grep、按行读、apply_patch（多文件补丁）、diagnostics(tsc) | `a7749f2` |
| 沙箱 | 子进程 + 密钥擦除 + 关 stdin + 超时杀进程树 + 输出上限 | `e0b1c77` |
| 多文件记忆 | Claude-Code 式：MEMORY.md 索引常驻注入 + 按需 memory_read，双作用域 | `d09b0c3` |
| 计费 | DeepSeek 真实价（命中/未命中/输出），显示 ¥ | `0b442b0` |
| Ink TUI | 全屏 TUI：火焰 banner、滚动区、可折叠推理、钉底仪表盘、Markdown/表格 | `0b442b0`/`9e58d1d`/`39c169e` |
| 设计语言 | 暖色单一来源 `ui/theme.ts`（白·黄·橙·橙红）、二维火舌 banner、TUI 英文化 | `39c169e`/`e5f9771` |
| diff 显示 | 写工具结果渲染 Claude-Code 风格 diff（LCS + hunk + 暖色） | `2f6a896` |
| 权限 | /pass-permissions 旁路 + HARD_DENY 黑名单（rm -rf / fork bomb 等）兜底 | `72d21dd`/`75ac86e` |
| 命令历史 | 输入框 ↑/↓ 翻历史 | `ebe14fd` |
| 生产硬化 | 测试地基（node:test 零依赖）、API 重试退避 + 错误分类、config schema 校验 | `1bb18de`/`d607e76`/`97a4228` |

**学到**：库的 `AgentHarness` 把 agent 主循环、会话树、压缩原语都给了；我们做的是**应用层**
（权限/审计/可观测/工具/UI/上下文策略）。`tool_execution_start` 在确认钩子**之前**发——所以
spinner 不能在工具执行时启动（会盖住 [Y/n] 提示假死）。

---

## 主题一：上下文工程 & 自研压缩（让 forge 能超长运行）

**核心约束（先想清楚再动手）**：DeepSeek 是**纯前缀磁盘缓存、无 Cache Editing API**
（查证：api-docs.deepseek.com/guides/kv_cache）。命中缓存便宜 40×、快。推论：

- **单 session 严格 append-only**：进了上下文就别改，否则破前缀缓存、整条重算。
- **破缓存的改写都搭车在 compaction 这一次重建上**；两次压缩之间纯追加。
- **compaction 少而狠**：90% 高水位单次触发，一次释放一大片（留近端 20% 窗口），
  让一个 session 里的「重建次数」最少。
- ⟹ 因此**砍掉了 Claude Code 的「微压缩」层**：它依赖 Cache Editing API 删工具结果，
  DeepSeek 给不了。改为「输入侧精打细算」——工具结果产出即截断。

| 步骤 | 做了什么 | 坑 / 学到 | commit |
|---|---|---|---|
| 缓存命中率 | 仪表盘显示 prompt cache 命中率（cacheRead/总 prompt） | append-only 下命中率 0%→90%+ 单调爬升是**正常**（首轮无前缀，之后旧前缀越占越大） | `4e33c44` |
| 工具结果截断 | 新 `kernel/artifacts.ts`：超 8KB 截断留首尾，完整内容落 `.forge/artifacts/`，指针引导 | 截断发生在「进上下文前」→ 不违反 append-only | `f2b0e6b` |
| 自研完整压缩 | 新 `kernel/compaction.ts`：turn 边界 cut（不拆 tool 对）、9 段摘要、map-reduce 兜底 | 库的 `compact()` 写死默认设置；9 段含「全量用户消息」防漂移；去掉 `<analysis>` 标签（DeepSeek 有 reasoning 通道） | `c8773bc` |
| 接管 + 触发 | `session_before_compact` hook 返回自研结果（不 fork 库）；90% 触发 + 熔断 3 次 + 子 agent 排除 | hook 返回完整 `{compaction}` 即可，库负责落会话树 | `06a8da4` |
| 截断指针 | 指针明确「用 read_file offset/limit 按行精准读 artifact」 | 精准读取复用既有 read_file，无需新工具 | `4b080d9` |
| rehydrate | **搁置**（用户决策）：压缩后重注入文件全文先不做 | DeepSeek 非顶尖，往上下文猛塞反增幻觉；干净精炼 > 塞满。代码 TODO 在 `runCompaction` | `6135adf` |

**关键设计句**：*平时 append-only 吃满 cache，要破缓存就攒到 compaction 一次性做完，且 compaction 少而狠。*

---

## 主题二：交互打磨

| 步骤 | 做了什么 | 坑 / 学到 | commit |
|---|---|---|---|
| /compact 进度 | 长操作经 `onStatus → bridge.status` 驱动动态状态行（spinner + 阶段 + 计时）；压后仪表盘即时刷新 ctx | LLM 调用没法给真百分比 → spinner + 阶段文案才是「动态显示在干嘛」的正确形态 | `5dcc96c` |
| 斜杠命令菜单 | 打 `/` 弹全部命令，子串模糊过滤、↑↓ 循环选、Tab 补全、Enter 执行选中项 | 抽纯函数 `ui/commands.ts` 可测；ink-text-input 对 Ctrl+C/Tab 已 early-return，不抢键 | `5dcc96c` |
| 读取显示行范围 | read_file 表头 `read_file(path · L10–29)`、结果行 `read N lines (L..)`；markdown 配色去蓝 | 蓝色来自 markdown 的 cyan → 改金/琥珀，收进暖色 | `8a61846` |
| Ctrl+C 分级 | 运行中=中止回输入 / 确认中=拒绝 / 有输入=清空 / 空输入连按两次=退出 | 关掉 Ink `exitOnCtrlC` 自己接管；决策抽纯函数 `ui/keybinds.ts` 可测 | `6deb1ef` |
| TUI 英文化 | 框架 chrome 统一英文（助手回复仍跟随用户语言） | — | `3512745` |

---

## 主题三：跨项目能力

| 步骤 | 做了什么 | 坑 / 学到 | commit |
|---|---|---|---|
| 只读越界开关 | `allowReadOutsideWorkdir`（env/config）：只读工具可读 workdir 外绝对路径，**写仍锁死 workdir** | 审查别的项目时 read_file 被 workdir 锁住 → 模型被迫退回 bash Get-Content。开关后能直接 read_file。写越界恒拒（单测断言） | `92ba974` |
| 环境提示 | 系统提示注入本机特征（Windows/PowerShell + 工作目录 + 避坑） | 模型常不知在 PowerShell 里 → 写 `&&`/`$VAR`/`ls` 撞墙。内容静态 → 不破缓存 | `525117a` |

---

## 主题四：code-intel（先看结构，别整文件通读）

**动机**：模型审仓时每个文件读几百上千行。业内（Aider repo map / AFT / Zed / Claude Code
2025-12 加的 LSP）已转向「**outline-first**」。对 DeepSeek 尤其能省上下文、降幻觉。

| 步骤 | 做了什么 | 坑 / 学到 | commit |
|---|---|---|---|
| outline（语法/单文件） | `kernel/code-outline.ts` + `tools/outline.ts`：tree-sitter 抽符号+起止行+签名 | **web-tree-sitter 必须锁 0.22.6**（默认 0.26 与 `tree-sitter-wasms`(cli 0.20.8) ABI 不兼容，报 getDylinkMetadata）；用默认导出 API | `dc1b260` |
| repo_map（全仓地图） | `tools/repo-map.ts`：遍历源码逐个 outline → 文件→符号签名地图，token 预算约束 | 复用 `search-tools.walk`；单文件最多 40 符号防吃光预算 | `65f7433` |
| 大文件截断纠偏 | 截断文案 `long lines truncated` → `⚠ truncated: middle omitted`；强化「大文件先 outline 再按行读」 | 实测发现：`read_file · full` 大文件超 8KB 被砍中段，旧文案误导模型以为读全了 → review 完整性风险 | `e4c5ada` |

**学到**：tree-sitter 是**语法级**（单文件，知道"有哪些函数"），不懂跨文件语义——所以叫"阉割版"，
引出主题五。

---

## 主题五：LSP（语义/跨文件，code-intel 的增强层）

**tree-sitter vs LSP**：前者语法（单文件），后者语义（全工程符号图 + 类型）：go-to-definition、
find-references 跨文件、准、快（find-references ~50ms vs 文本搜 ~45s）。

**安装即"装语言服务器 + 写客户端"**（LSP 是协议不是软件）：forge 选 **npm 自包含**的
pyright（py）/ typescript-language-server（ts/js），无需 Python/Go 工具链。

| 步骤 | 做了什么 | 坑 / 学到 | commit |
|---|---|---|---|
| 客户端内核 | `kernel/lsp-client.ts`：按扩展名 spawn server、JSON-RPC over stdio、initialize/didOpen/shutdown；封装 documentSymbols/definition/references/hover + 收集 diagnostics；server 缺失优雅降级 | **别单独装 `vscode-jsonrpc`**（与 protocol 内置的双副本 → "Unknown parameter structure byName"）；用 `vscode-languageserver-protocol/node` 的 `createProtocolConnection`。server 经 `node + require.resolve(...langserver.index.js)` spawn（Windows 友好） | `1f56375` |
| 语义工具 | `tools/lsp-tools.ts`：definition/references/hover 按**符号名**查询（locateSymbol 定位→LSP 位置查询）；接入 agent + dispose() 关子进程 | 模型不知道字符位置 → 工具按符号名，先在文件里词边界定位再查 | `87b5c7e` |
| 跨文件漏报修复 | warm()：首次查询前预热全工程同语言文件让 pyright 索引完再查 | **实测**：references 只返 1（漏调用点）——客户端只 didOpen 被查文件，pyright 惰性分析。预热 → 1 变 2。顺带修 Windows 盘符大小写、teardown EPIPE | `dc1274c` |
| diagnostics 升级 | 统一 `diagnostics(path?)`：py/ts/js 有 path → LSP 单文件语义诊断；否则回退 tsc 整项目 | **URI key 不匹配**：我方 `file:///C:/…` vs pyright `file:///c%3A/…`（盘符大小写+编码）→ 诊断 Map 按归一化文件路径(win 小写)做 key，否则漏报全部。pyright 先推空诊断再推最终 → 等**趋稳**再取 | `1b24019` |

**学到**：LSP 的结果质量取决于「server 是否索引完工作区」——惰性分析 + 异步推送是两大坑，
预热 + 趋稳等待是关键。

---

## 主题六：打磨与闭环（实测驱动）

| 步骤 | 做了什么 | 坑 / 学到 | commit |
|---|---|---|---|
| diagnostics 升级 | 统一 `diagnostics(path?)`：py/ts/js 走 LSP 单文件语义诊断，否则回退 tsc 整项目 | **URI key 不匹配**（我方 `file:///C:/…` vs pyright `file:///c%3A/…`）→ 诊断 Map 按归一化路径(win 小写)做 key；pyright 先推空诊断再推最终 → 等**趋稳**再取 | `1b24019` |
| apply_patch CRLF | parsePatch 按 `/\r?\n/` 切分 | 工具巡检逮到：Windows CRLF 残留 `\r` 让 `*** File` 标记正则全线失败 | `…` |
| 记忆收紧 | memory_write 引导加 ✅该记/❌不该记 + 系统提示克制条 | 模型曾把「巡检结果」写成 long-term memory，索引行每会话注入 → 新会话复述 stale 结论。失败模式是「记太多」 | `…` |
| A1 工具显示 | outline/repo_map/definition/references/hover/diagnostics/rename 显示干净摘要 | 之前落到通用 `clip(JSON)` → TUI 显示截断 JSON | `…` |
| A2 编辑后自动诊断 | 写类工具成功后 tool_result hook 自动 didChange + diagnostics，有 error 追加进结果 | `opened` 改 `Map<uri,version>` + `didChange()`：编辑后不通知 server 会漏报（持旧内容） | `…` |
| 压缩阈值可配 | `maxContextTokens` 当「虚拟窗口」，触发(0.9)与保留(0.2)都按它算 | 该字段早就在 config.json（8000）但**从没接线**（不在校验白名单被静默丢）；1M 窗口压测太贵 → 降 200K 验证 | `…` |
| LSP rename | 跨文件重命名：归一化 WorkspaceEdit + applyTextEdits（倒序套、CRLF 安全）+ didChange 同步 | 顺带修：只读 code-intel 工具没进权限 allowlist → 没 bypass 时每次弹确认 | `…` |

---

## 关键经验 / 踩坑速查

1. **缓存优先**：单 session append-only，破缓存的改写攒到 compaction 一次做。
2. **少喂上下文**：工具结果截断、outline-first、大文件别 `· full`（会被砍中段）。
3. **web-tree-sitter 锁 0.22.6**（匹配 tree-sitter-wasms 的 ABI）。
4. **LSP 客户端**：用 `vscode-languageserver-protocol/node`（别单装 jsonrpc）；spawn 用 `node + 入口 JS`；
   URI 按归一化路径(win 小写)做 key；查询前预热工作区；诊断等趋稳。
5. **Windows 特性**：PowerShell 语法提示进系统 prompt；路径盘符大小写到处要小心；
   **文本解析按 `/\r?\n/` 切分**（apply_patch 曾因 CRLF 残留 `\r` 全线解析失败，`commit dc…` 后修）。
   → 一次「全工具巡检」让模型交叉验证 16 个工具，逮到了这个 apply_patch 的 CRLF bug（其余全过）。
6. **每个功能配单测**：纯逻辑抽出来测（formatX/locateSymbol/ctrlCAction…），重逻辑用真 server 集成测。
7. **记忆要克制**：MEMORY.md 只存索引行（name+一句话 description，每会话注入），原文在 `<name>.md` 按需 memory_read；失败模式是「记太多」——只记会改变未来决策的持久事实，别记一次性结果（曾因模型把巡检结果写成记忆，导致新会话复述 stale 结论）。

---

## 主题七：子 agent 编排演进（2026-05-26，实测驱动的多轮迭代）

起点设计本就对的部分：隔离会话 + 受限只读工具 + **无 spawn 工具(防递归)** + abort 透传 + 失败兜底不崩主流程。
随后由实测一轮轮打磨：

**① review 发现的隐性问题**：`thinkingLevel` 漏传 → 子 agent 静默走库默认(reasoning off)，与主 agent 不一致。
→ 显式改用 **deepseek-v4-flash + thinking**(调研类省成本，解析失败回退主模型)；描述去掉误导的"并行化"；失败补 audit。

**② 护栏 + 能力 + 清理**：加 **max-turns 护栏**(订阅 turn_end 计数，超上限即 abort——库无此护栏)；
**实时状态**挂仪表盘下方；**能力套用主 agent**(加 repo_map + LSP definition/references/hover，共享 LspClient)；
**会话清理**(repo.delete 不堆积)。压缩对子 agent **moot**：一次性 agent(单 prompt 跑完即弃)无跨轮累积可压，
max-turns + outline-first 即边界。

**③ 轮数预算交给主 agent**：maxTurns 由主 agent 按复杂度定(简单 5-10 / 复杂 30-50 / 0=不限 / 省略=25)，不再写死；
返回结论时附执行信息(轮数 / 工具数 / 是否截断)，主 agent 看得到子 agent 干了多少活。

**④ 异步/后台 + 不阻塞主 agent（核心）**：这是反复踩坑后的终态。
- *为何异步*：同步 spawn 会阻塞主 harness、期间 LLM 停着，既没"中途撤销"的时机，用户打字还报 `AgentHarness is busy`。
- *为何不要阻塞等待*：`subagent_result`(无限阻塞) / `subagent_wait`(有界阻塞) 都仍占住主 harness → 治标不治本。
- **终态 = fire-and-forget + 事件驱动回收**：`spawn_subagent` 立即返回 id、主 agent 这轮即结束(不占)，可并行派多个；
  子 agent 完成时把结论**作为新一轮自动喂回**主 agent(`onResume`)；App 用**串行 run 队列**统一调度用户输入与
  子 agent 回灌(单线、永不撞 busy，连 busy 时提交也入队)。`subagent_list` 看状态 + **运行日志**(最近 turn/工具事件 +
  已运行秒数)判断推进/卡死，`subagent_cancel` 撤销。卡死靠状态行 + 用户介入，不用阻塞兜底。
- *教训*：REPL 里 `followUp`/`steer` 在 idle 抛错、`nextTurn` 入队要等下次 prompt 才排空——所以"完成回灌"必须在
  **App 层用串行队列**驱动，不能指望 harness 的队列。

**⑤ flash 用量单独计量**：子 harness subscribe 到独立的 `subTelemetry`；仪表盘**另起一行**显示子 agent 用量
(真实模型名 + turns + token + cache% + ¥)。顺带按官方价修正定价：**Pro 未命中 ¥1→¥3**(之前错的，2.5 折价)、
新增 **Flash ¥0.02/¥1/¥2**(命中/未命中/输出)。

## 主题八：Windows 兼容修复 + 飞行记录仪（2026-05-27）

### 8.1 pi-agent-core 的 Windows `/`-only 路径 bug

**为什么**：用 forge 给自己装第一个 skill（`.forge/skills/karpathy-guidelines.md`）就崩：
`Fatal: path should be a path.relative()'d string, but got "C:\...\karpathy-guidelines.md"`。

**根因**：`@earendil-works/pi-agent-core@0.75.4` 的路径处理全假设 POSIX 正斜杠，
而 `NodeExecutionEnv` 经 `path.resolve` 在 Windows 返回 `\` 反斜杠路径。两处症状：
1. `skills.js` 的 `relativeEnvPath` 用 `startsWith(root + "/")` 削前缀，`\` 路径削不掉 →
   把绝对路径当相对路径喂给 `ignore` 包 → 包严格拒绝绝对路径直接 throw。（空目录不触发，
   放进第一个文件才炸。）
2. `nodejs.js` 的 `fileInfoFromStats` 用 `path.split("/").pop()` 取 basename，`\` 切不开 →
   `entry.name` 退化成完整路径 → `entry.name !== "SKILL.md"` 永真 → 子目录 skill 永不被识别。

**怎么做**：`kernel/skills-win.ts` 包一层 Proxy env——把 `fileInfo`/`listDir`/`canonicalPath`
返回的 `path` 归一成 `/`，并按归一后路径**重算 `name`**（Node 在 Windows 也吃正斜杠，fs 操作不受影响）；
`forge-agent.ts` 用 `loadSkillsCrossPlatform` 取代库的 `loadSkills`。POSIX 平台是恒等变换、零副作用。

**学到/坑**：① 跨平台路径库最隐蔽的 bug 是「只分割 `/`」——Windows 上静默产出错误结果而非报错；
② skill 须有 frontmatter `description:` 才会加载，标准布局是 `.forge/skills/<name>/SKILL.md`（name 配目录名）；
③ 同一假设可能潜伏在库的会话存储等其他 Windows 代码路径，撞上照搬本文件的 toPosix/baseName 思路。

### 8.2 飞行记录仪（Flight Recorder）

**为什么**：要拿飞书二次开发任务跑 `/converge` 实战压测整个 harness（含 autocompact），
需要**完整、未截断**的日志事后复盘优化——尤其想对比 compact 前后看压缩设计够不够好。
现有 `AuditLog` 截断到 300 字，做不了这事。

**怎么做**：`kernel/flight-recorder.ts`，抽可测纯核 + 注入 sink：
- `serializeEvent`（纯函数）：harness 事件 → JSON 安全记录（或 null 丢弃），可单测。
- `safeStringify`：Set→数组 / 函数·AbortSignal 丢弃 / 循环引用标记 / BigInt→串（绝不抛错中断 agent）。
- `FileFlightSink`：持久 fd + `writeSync` 逐行同步落盘（durable，崩溃也不丢最后一行）。
- `FlightRecorder`：订阅 harness 完整事件流（只读观察），维护 seq + 「压缩后下一条 context 整条 dump」状态机。

**与 AuditLog 分工**：AuditLog = 截断的合规留痕 + 内存环形缓冲（喂 UI 近况）；flight = 全量 trace
（工具结果 / 模型回复 / 压缩前后全 verbatim），离线分析 / 压测 / 未来 resume 的观测底座。

**捕获内容**：用户输入(before_agent_start) / 模型回复(message_end) / 工具调用+结果(tool_call·tool_result，
content 完整不截断) / compact 前(session_before_compact 全量 branchEntries + tokensBefore) / compact 后
(session_compact 摘要) + 钩子内精选 **`compact_detail`**（待压全文 + 产出摘要 + keptTokens，一条 grep 对比压缩质量）。

**context 体积策略（与用户对齐）**：默认 `summary` = 增量流 + 每轮 context 摘要行（消息数/token/角色分布）
+「会话首条 & 每次 compact 后第一条」整条 dump（线性增长，足够复盘）；`FORGE_FLIGHT_CONTEXT=full`
则每轮整条 dump（最高保真但随会话二次膨胀，压测要榨干每比特时用）。硬丢弃 `message_update`/
`tool_execution_*`/`before_provider_payload` 等流式增量/二次膨胀噪声。

**落点**：每次运行一文件 `.forge/flight/<ts>-<sessionId>.jsonl`（`.forge` 已 gitignore）；
默认开，`FORGE_FLIGHT_LOG=0` 关；`agent.flightLogPath` getter 暴露落点。

**注意**：resume 的**数据**底座本就是 session JSONL（`JsonlSessionRepo`/`repo.open`，`/resume` 已用），
flight 是**观测层**不重复造数据。

**子 agent + Convergent 也接了**：它们走 `runEphemeralAgent` 里独立的 `sub` harness。`FlightRecorder.scope(tag)`
开一个带标签的**子流**——每条记录打 `agent` 字段（`main` / `subagent:<role>#<id>` / `convergent`），
各子流持有**自己的 context 状态机**（否则共享 `dumpNextContextFull` 会互相干扰），但共享单调 `seq` 写同一文件。
`runEphemeralAgent` 收一个 `flightTag`，传了就把该子 harness 的全量事件 `subscribe` 落盘；`runSubAgentLoop` 传
`subagent:<role>#<id>`、`runConvergent` 传 `convergent`。复盘时按 `agent` 字段过滤即可分离主/子/验收三路 trace。

测试：`test/flight-recorder.test.ts` 15 例（硬丢弃 / 不截断 / context 摘要vs全量 / 压缩前后 /
safeStringify 边界 / 状态机 summary·full 模式 / **agent 标签 / scope 子流独立状态机共享 seq** / record·close）。
全套 168→183 passed，typecheck clean。

## 主题九：内置 SSH 工具（2026-05-27）

**为什么**：飞书二次开发压测要远程部署/看日志。先评估「bash 硬连」——发现沙箱有几处实打实的坑：
① bash 的 stdin 被关成 EOF → 任何交互提示（密码/host-key/sudo）直接失败；② **写边界守卫
（`detectWriteEscape`）只认命令字符串、分不清本地/远程**，会把 `ssh host "x > /etc/y"`、
`ssh host "rm -rf /tmp/z"` 这类正常远程命令当越界写**误杀**（除非整个关掉守卫，但那样本地写也没护栏）。
用户拍板：加专用工具，config 闸住。

**怎么做**：
- **抽 `spawnCaptured(file, args, opts)`**（`sandbox/exec.ts`）：把原 `execSandboxed` 的
  spawn+捕获+杀进程树+超时+限流抽成 argv 形态的通用核，bash 与 ssh 共用。**直接 spawn `ssh` + argv，
  不经 PowerShell**，远程命令作为单个 argv 元素 → 免去本地 shell + 远程 shell 的双重引号转义。
- **`tools/ssh.ts` 的 `ssh_run`**：参数 `{ profile, command, timeoutMs? }`。`buildSshArgs`（纯函数可测）
  统一 `StrictHostKeyChecking=accept-new`（首连不卡）+ `ConnectTimeout=10`，两种认证模式：**密钥/默认**走
  `BatchMode=yes`（绝不提示）+ `-i` 密钥（`expandHome` 展 `~`）；**密码**（见下）开交互 + 强制密码认证。
  `-p` 端口 / `user@host`。未知档案返回可读错误不抛。
- **config `ssh`**：`forge.config.json` 的 `ssh.<name> = {host,user?,port?,key?,password?}`，
  `validateConfigFile` 校验（缺 host / 端口越界 / 未知字段即抛）。只挂主 agent，不给子 agent / Convergent。
- **注册策略（实测后修正）**：起初「非空才注册」→ 用户没配档案时工具**隐身**，模型报「没有 ssh 工具」造成困惑。
  想清楚后改为**常驻注册**：真正的授权边界是「`profile` 必须匹配配过的档案」这条参数约束，**不是工具是否注册**——
  未配置时常驻的 ssh_run 调用只会返回引导错误（「未配置任何 ssh 档案，请在 forge.config.json 的 ssh 段添加…」），
  连不上任何 host，安全性不变、但模型能发现这个能力。description 与未知/空档案错误都按是否有档案分别给文案。
- **密码认证支持（SSH_ASKPASS 原生，实测踩坑后加）**：起初 ssh_run 只支持密钥，用户把 `"password"` 写进档案被
  `validateConfigFile` **静默丢弃** → 叠加 `BatchMode=yes` 禁密码 + 无私钥 → ssh exit=255，模型还误诊成「config
  权限问题」去 icacls 瞎搞（**对象都找错**：Windows OpenSSH 严校验的是私钥文件不是 config，且就算改完权限没凭证照样 255）。
  用户诉求：内网机密码直连更省事（内网泄露无所谓、公网才用密钥）。**Windows 关键事实**：原生 `ssh.exe` 故意不收
  命令行/管道密码；但 OpenSSH 8.4+ 支持 `SSH_ASKPASS` + `SSH_ASKPASS_REQUIRE=force` 用「密码提供器」喂入——**原生、
  不用装 plink/sshpass**。实现：密码档案走 askpass（一个 `.cmd`/`.sh` 助手，从环境变量 `FORGE_SSH_ASKPASS_PW`
  读密码并原样输出——**密码只在子进程 env、不落盘不进脚本**，彻底避开 batch 对 `& ! % ^ $` 的引号地狱），
  并去掉 BatchMode（askpass 要靠提示喂）、强制 `PreferredAuthentications=password,keyboard-interactive`+`PubkeyAuthentication=no`。
  实测连真实内网机 exit=0、特殊字符密码 OK。教训沉淀：**不支持的字段要报错而非静默忽略**——validateConfigFile 现在
  接受 password、拒绝未知字段。
- **安全事故复盘 + 处置**：明文 root 密码一度进了 **git 跟踪**的 forge.config.json（幸未提交进历史）+ 被 LLM 读进上下文。
  处置：把 `forge.config.json` `git rm --cached` + 加进 `.gitignore`（机器私有配置、含密码，永不进 git——用户公开仓库多）；
  磁盘保留（内网威胁模型可接受）。**诚实边界：密码明文存配置 + 进 agent 上下文，仅限可信内网；公网必须密钥。**
- **权限**：`ssh_run` 不是 `bash` → 天然**不走本地写守卫**（正是要的）；但 `permission.ts` 加了一条——
  **HARD_DENY 黑名单也覆盖 ssh_run 的远程命令**（连 prod 也别 `rm -rf /`，即便 `/pass-permissions`
  自动放行时也拦）。默认每条远程命令仍需用户确认 + 全量审计 + 飞行记录（带 tool_call/tool_result）。

**诚实边界**：ssh 是**伸到沙箱外、那台机器无任何守卫**的能力，比本地 bash 更强；护栏只有「host 白名单
（config 档案）+ HARD_DENY + 用户确认」。认证：密钥（公网/不可信）或密码（仅可信内网，明文存配置 + 进上下文）。
首连 host key 走 `accept-new` 自动记（不卡）。stdout/stderr 合并返回（连接/认证错误在 stderr，模型看得到）。

测试：`test/ssh.test.ts`（buildSshArgs 密钥/密码两模式 / expandHome / 未知/空档案）+ permission/config 各补若干例；
真盘 smoke：spawn 真实 ssh.exe 连不可达 host → 2s 内 exit=255「Connection refused」不挂死。全套 183→192 passed。

## 主题十：多行输入框（替换 ink-text-input，2026-05-27）

**为什么**：用户反馈两个输入框问题——① 粘贴多行文本**显示糊掉**（内容没丢，一个字一个字删能删全，
纯渲染问题）；② 多行时按 ↑/↓ 一律翻命令历史，而非在行间移动光标。根因同一个：
`ink-text-input` 是**单行**组件，存得下含 `\n` 的值但渲染按单行处理 → 错乱；↑/↓ 又是 app 自己
`useInput` 接的（ink-text-input 对上下键直接 return）→ 永远翻历史。

**怎么做**：自研多行输入，零新依赖。
- **纯核 `ui/text-editor.ts`**（全可单测）：`{text,cursor}` + insert/backspace/moveLeft/Right、
  `cursorRowCol`/`lineStarts`、`moveUp`/`moveDown`（**首行↑/尾行↓返回 null** = 信号让调用方翻历史，
  否则保持列夹到目标行长移动）、`lineHome`/`lineEnd`、`normalizeNewlines`（CRLF→LF）。
- **组件 `ui/multiline-input.tsx`**（薄层）：受控 `value`（父持有，供菜单匹配/占位/清空/历史召回），
  cursor 内部维护；**外部改 value 自动把光标移末尾**（替代旧的 inputKey 重挂 trick）。useInput 映射键 →
  纯核：回车提交 / Alt·Shift+回车插换行 / ↑↓ 行内移动或边界翻历史（回调 onHistoryPrev/Next）/
  ←→ / Backspace（兼容 key.delete）/ Ctrl+A·E 行首尾 / 可打印含**批量粘贴整段插入**（ink7 粘贴批量
  投递为一段含 \n 的 input，不会逐行误提交）。渲染 = before + 反显光标块 + after（光标在换行/末尾用空格块代显）。
- **app.tsx**：菜单 useInput 改为**仅菜单开时**接 ↑↓/Tab（菜单关时这些键归输入组件）；删掉历史分支，
  抽出 `historyPrev`/`historyNext` 回调；`histIdx` 改 **ref**（不参与渲染，避免 stale 闭包 / setState 内副作用）；
  删掉 `inputKey` 全部用法（光标复位由组件接管）。

**边界**：Alt/Shift+回车插换行依赖终端能区分（多数终端 Shift+Enter 与 Enter 同码，不一定生效）——
但**粘贴的换行始终可用**（走 input 整段），手动多行是次要场景。↑/↓ 按**逻辑行**（`\n`）判定，不感知视觉折行。
`ink-text-input` 依赖已不再 import（package.json 里可后续移除）。**TUI 渲染/手感无 TTY 自测不了，需终端实测。**

测试：`test/text-editor.test.ts` 10 例（光标夹取 / 换行归一 / 插入·退格 / 行列 / moveUp·Down 含
边界 null 与列夹取 / 单行恒翻历史 / lineHome·End）。全套 192→202 passed，typecheck clean。

## 主题十一：忙时插话 + diff 语法高亮 + 两个压缩 bug 根因（2026-05-28，飞书压测实测驱动）

拿飞书二次开发任务实跑 `/converge`，暴露并修掉了两个**真·压缩 bug**；顺带补了交互与观感。
全套 205→220 passed，typecheck clean。4 个 commit：`8769e7a`·`41412c3`·`378f8c9`·`726e0c4`。

### 11.1 忙时输入走 steer（不打断当前步的插话，`8769e7a`）

**为什么**：之前 `RunQueue` 把所有用户输入当**整任务**串行化——agent 正忙时回车，要等整个任务收敛
才被消费。想要「他思考完/工具调用完，下一步立刻收到我的话」，而非等全任务结束。

**怎么做**：发现底层 `AgentHarness` **原生支持 steering**（`harness.steer(text)` + `steeringMode`，
库在「当前 turn 工具执行完、下一次 LLM 调用前」注入排队消息）。改 `RunQueue` 加 `submit()`：
**忙**（有 run 在跑）→ `agent.steer` 注入当前 run；**闲** → 照旧 `enqueue` 新任务。`steeringMode:"all"`
让连发多条在同一 turn 边界一次性注入。后台子 agent 回灌仍走 `enqueue`（新 run）不变。

**边界/学到**：注入粒度是 **turn 边界**（不是单个工具执行到一半打断——那样会撕裂 tool_use/tool_result 对）；
所以「工具跑到一半」插的话要等这批工具跑完才送达，但绝不用等到整个任务结束。

### 11.2 diff 语法高亮 + 增删色带 + 提亮过暗灰字（`41412c3`）

**为什么**：用户反馈 diff 整块刷黄太丑（想要像编辑器那样按 token 分色 + 红/绿增删），且部分灰字
（用 ANSI `dim` 属性 `\x1b[2m`）被多数终端压得过暗、糊成一团。

**怎么做**：新建 `ui/highlight.ts`——零依赖语法高亮器（注释/字符串/数字/关键字/常量/函数/类型分色，
支持 ts/js/py/go/rust/java/c/cpp/sh/json/yaml/css），**token 一律用 `\x1b[38;5;Nm…39m` 只重置前景**
（不用 `\x1b[0m`），以便叠加在 diff 行背景色带之上不抹掉背景；`langForPath`/`normalizeLang` 推断语言。
`ui/diff.ts`：新增行铺**深绿底**、删除行铺**深红底**（Claude Code 风格整行色带，truecolor `48;2;…` + 补空格铺满
`contentWidth`），代码语法高亮，`+`/`-` 标记亮绿/亮红。`ui/markdown.ts`：`dim` 从 `\x1b[2m` 属性换成
**浅灰色号 `38;5;250`**（表格框线/引用/分隔线一起提亮）；代码块从「整块压暗」改为**语法高亮**（带语言标注按语言、
无标注按纯文本亮色渲染——修复树状图等发灰）；导出 `visibleWidth` 供色带对齐。

**坑/学到**：背景色带内**绝不能用含 `\x1b[0m` 的 ansi.dim**（会把背景抹掉）——行号/标记都改用 `…39m` 收尾、
整行末尾才统一 `RESET`；引入紫/绿/青冷色是刻意突破原「纯暖色」设计语言（用户明确要编辑器式分色），
想收回改 `highlight.ts` 顶部 `COL` 一处即可。

### 11.3 压缩 bug ①：ctx 不降 + 重复触发（触发判定读到陈旧 usage，`378f8c9`）

**症状**：autocompact 后仪表盘 ctx 卡在 187.8k 不降，每发一条消息又再次触发压缩。

**根因（飞行日志 + session 树双重实证）**：`lastContextTokens`（驱动压缩触发 + 仪表盘）原在压缩前的
`on("context")` 钩子里用库的 `estimateContextTokens(messages)` 算，而该函数把上下文大小**锚定在
「最后一条带 provider usage 的 assistant」**上。压缩后保留窗口里**残留着压缩前那条满窗 usage 的
assistant（usage.input≈187k）**，下一轮在生成新 assistant *之前*触发的钩子又读到它 → 用量被算回 187k。
（实证：压缩确实把真实上下文从 187827 降到了 36789——库自己在下一次压缩时算出的 `tokensBefore=36789` 为铁证；
坏的只是这个**触发读数**。）

**修复**：① 删掉 `on("context")` 里的赋值，改在 **`turn_end`** 用该轮 assistant 的**真实 usage**
（`calculateContextTokens`）记录——压缩后自然变小，不被残留旧 usage 顶高。② `runCompaction` 在
`compact()` 成功后**即时重算** `lastContextTokens = estimateTokens(摘要) + keptTokensFrom(branch, r.firstKeptEntryId)`，
仪表盘当场下降；用实际生效的 `firstKeptEntryId`（自研摘要 / 库默认回退两条路径都对）。新增纯函数 `keptTokensFrom`。

### 11.4 压缩 bug ②：自研 9 段摘要永不生效（assistant 体量被低估，`726e0c4`）

**症状**：飞行日志显示每次压缩 `fromHook` 都是 false——**自研 9 段摘要从没跑过，一直退回库默认 6 段**。

**根因**：`textOfMessage`（`toPlanEntries` 用它序列化消息给压缩计划）对 assistant 消息只取 `text` +
`[tool_call 名字]` 占位，**丢弃了 thinking 与 tool_call 参数**——而这两块正是编程 agent assistant 消息的主体
（write_file 全文 / patch / 命令 / deepseek 推理 max 的大段思考）。后果：token 估算严重偏小（实测同一会话
assistant 全部只估出 **~7k**，真实 **~180k**；整段估成 45k）。当估算总量 ≤ keepRecent(40k) 时，
`selectCutPoint` 累加永远够不到预算 → `idx` 停 0 → 对齐后 **`cut=0` → `extractCompactionPlan` 返回 undefined →
hook 返回 undefined → 每次退回库默认**（这条 `!plan` 分支不写 audit、不写 compact_detail，正好对上「无错误痕迹」）。

**修复**：`textOfMessage` 的 assistant 分支**计入 thinking 文本与 tool_call 参数(JSON)**；无参数时仍输出
`[tool_call 名字]`（保旧行为）。新增 `safeJsonForText` 兜底序列化。计入后该会话估算回到 ~126k、cut 落点正常、
产出有效 plan，自研压缩得以运行。

**两个 bug 的关系**：同一条压缩链上、**相互独立**的两处——①修「触发/仪表盘读数」，②修「压缩计划选点」。
合起来压缩才**端到端**正常：正确时机触发 → 用自研 9 段摘要 → ctx 当场下降 → 不再重复触发。
**学到**：可观测性是抓这类 bug 的命门——飞行日志的 `fromHook` 字段 + `compact_before/after` 的 `tokensBefore`
+ session 树 dump，直接把「压缩生效了但读数没跟上」和「压缩计划根本没产出」两个相互掩盖的现象拆开了。

---

## 主题十二：Skills 管理机制（2026-09-08，算子工具转型第一步）

**背景**：forge 转型「算子开发专用 AI 编程工具」。先派 8 个调研 agent 深扫 GitHub/GitCode/Gitee
（昇腾/Triton/天数/沐曦/燧原/海光/NVIDIA/AMD，报告落 `docs/research/`），再出计划书
`docs/skills-management-plan.md`，然后 P0→P4 一次做完（每阶段独立可测，354 用例全绿）。

**核心架构决策（物理三态 × 逻辑组合，对「按芯片分区」四个矛盾的回答）**：
不平衡/软链接/重复读/更新断层 → 物理维度取抽象层级（common 自建通用层 · delta 自建差异层 ·
vendors 上游快照+lock），芯片退化为 `targets/*.yaml` manifest 声明组合（`target:` 可传递、禁环）。
海光 = target:amd + delta:hygon：借 AMD 家底不复制一行；多芯片激活时物理去重让 Triton 通用知识只注入一次。

| 阶段 | 做了什么 | 坑 / 学到 |
|---|---|---|
| P0 IR+扫描 | `kernel/skills.ts`：自研同步扫描器（SKILL.md/openai.yaml/Claude 字段/legacy 四格式识别归一）、manifest resolve（环检测、include 后者胜、filePath 去重）、renderIndex 分层裁剪（确定性输出） | 库的 loadSkills 丢完整 frontmatter（格式识别/invocation 归一需要原始字段）→ 自研比包一层更干净；yaml 是传递依赖，写进直接依赖锁 2.9.0 |
| P1 快照注入 | 索引与 memory 索引都在 create() 定格为字符串字段，systemPrompt 只读快照 | **修掉存量 bug：memory.indexBlock() 每次重读磁盘，memory_write 后整个前缀缓存作废**（计划书 Q6）。e2e 用「磁盘变更后两次 run 的 system prompt 字节相同」钉死 |
| P2 skill_read | 新工具（白名单=注册表、D7 会话内幂等：重复调用只回短注记、section 分段读、artifacts 截断）+ `/skills` 命令族（`/skills <name>` 把正文作为用户消息注入） | 幂等读集必须挂工具集实例（主/子 agent 上下文本就隔离），不能挂 registry；readonlyToolset 每次新建数组 → 实例经 create() 建一次传递 |
| P3 内置内容 | common 3 个通用 skill + 7 家 delta（内容全部来自调研报告事实）+ 10 个 target manifest + vendors lock（6 家 MIT/Apache）+ `npm run skills:fetch` | CANN OSL v2.0 非标协议内容不 vendor（cannbot 只借鉴结构）；未 fetch 的 vendor 降级 warning 提示 fetch，不崩 |
| P4 共享+摘要 | 子 agent/Convergent 的 systemPrompt 追加同一索引快照；压缩摘要第 10 段 Skills used（registry 记已用名单） | 摘要模板是静态字符串 → skillsUsed 走 buildSummaryPrompt 参数按需附加，无 skill 时不出现 |

**验证**：单测 46 个（skills/skill-tool/skills-fetch/compaction 追加）+ e2e 3 个
（快照字节稳定含磁盘变更、skill_read 走真实工具链的幂等、子 agent 拿到同一索引）。

---

## 下一步 / TODO

- **多行输入框终端实测**（用户）：粘贴你那段多行文本看显示是否正常；多行时 ↑↓ 行间移动、首行↑/尾行↓翻历史；
  Backspace / 中文宽字符 / 长行折行下光标位置；菜单（输 `/`）↑↓ 选择 + Tab 补全仍正常。
- **飞书 `/converge` 压测**（进行中）：已跑并暴露+修掉两个压缩 bug（主题十一）。**待复跑验证**：
  flight 里 `fromHook` 应变 **true**（自研 9 段摘要生效）、`ctx` 压缩后**当场下降**且不再重复触发；
  继续观察 Convergent 取证深度 / 写守卫误拦 / 自动回炉。
- **rehydrate**（搁置）：压缩后按预算重注入最近访问文件全文，需先建「最近访问文件追踪」。
- **更多语言 LSP（go/rust/java）**：outline 已支持（tree-sitter wasm 自带）；LSP 需各自的
  language server——`gopls`（要 Go 工具链）/ `rust-analyzer`（rustup）/ `jdtls`（JDK），不像
  pyright/tsls 能 npm 自包含。在 `lsp-client.ts` 的 `SERVERS` 加映射 + 探测二进制即可，缺失则
  优雅降级。**优先级低**（当前主要审 py/ts）。
- **编辑闭环验证**：让模型「改 → diagnostics 自检 → 修」跑通一个真实编辑任务。
- **CI / 分发**：取决于 forge 是否独立成自己的 GitHub 仓库。
